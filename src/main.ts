import { puppeteerProvider } from "./Providers/puppeteer";
import { Puppeteer } from "./Services/Puppeteer";

const Providers = { puppeteerProvider };
const Services = { Puppeteer };

export { Providers, Services };
